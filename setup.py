from setuptools import setup

setup(
    name=file_iterator,
    version='1.0.0',
    author='uvlasova',
    description='Разработка класса-итератора по файловой системе',
    zip_safe=False,
    packages=['file_iterator'],
)